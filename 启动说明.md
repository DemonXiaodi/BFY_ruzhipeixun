# 出口易新人培训平台 · 本地运行说明

## 最快方式：双击启动

双击项目根目录下的 **`start.bat`**。

脚本会自动检测运行时（Python → py → Node），启动服务并**自动打开浏览器**。
看到下面的日志就说明成功了：

```
Serving HTTP on 127.0.0.1 port 5180 (http://127.0.0.1:5180/)
```

**停止服务**：在黑窗口按 `Ctrl + C`，或直接关掉窗口。

---

## ⚠️ 重要：不能直接双击 index.html

项目里有三个用 `<iframe>` 嵌入的小游戏（十二条令 / 小鸟答题 / 模拟回复）。
用 `file://` 协议直接打开 HTML 时，浏览器的同源策略会拦掉 iframe，游戏区会**白屏**。

所以必须通过 HTTP 服务访问：`http://127.0.0.1:5180/index.html`

---

## 方式二：手动命令

如果你更习惯敲命令，在项目根目录打开终端，任选一条：

```bash
# Python（推荐）
python -m http.server 5180 --bind 127.0.0.1

# Node（用项目内自带的零依赖服务）
node server.js 5180
```

然后浏览器打开 <http://127.0.0.1:5180/index.html>

换端口：把上面命令里的 `5180` 改成别的数字即可（如 `8080`）。

---

## 端口被占用怎么办

如果 5180 提示被占用，换一个端口：

```bash
python -m http.server 8080 --bind 127.0.0.1
```

---

## 改代码后的热更新

这是纯静态项目，**没有构建步骤、没有打包**。

改完 `index.html` / `server.js` 或 `assets/` 下任何文件后，
只需在浏览器按 **`Ctrl + F5`**（强制刷新，忽略缓存）就能看到效果，服务不用重启。

---

## 文件说明

| 文件 | 作用 |
|---|---|
| `index.html` | 主应用，单文件 SPA（CSS/JS 全内联，6748 行） |
| `start.bat` | 双击启动脚本，自动选运行时 + 开浏览器 |
| `server.js` | 零依赖 Node 静态服务，Python 不可用时的兜底 |
| `assets/` | 图片素材与三个 iframe 小游戏 |
| `uploads/` | 设计参考图，**代码未引用**，可删 |
| `财务报销及合同审批.html` | 财务模块的早期独立版本，已被 index.html 取代，可删 |

---

## 需要发给别人看？

项目是纯静态的，可以直接部署到任意静态托管（GitHub Pages / Vercel / Nginx / OSS）。
把整个目录原样上传即可，**无需构建**。

也可以让 WorkBuddy 用「发布为应用」生成一个在线分享链接。
