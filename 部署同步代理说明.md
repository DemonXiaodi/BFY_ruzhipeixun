# 企业微信同步代理 · 部署说明（GitHub Pages / 静态托管场景）

## 为什么需要单独部署代理

企业微信智能表格的 Webhook **不返回 CORS 头**，浏览器在任何「非同源」页面里直接 `fetch` 它都会被同源策略拦截。
本地用 `node server.js` 起服务时，页面与代理**同源**（`/api/wecom-sync`），所以能转发。

但 GitHub Pages / Vercel / Nginx 等是**纯静态托管**，没有服务端，页面上 `POST /api/wecom-sync` 会被返回 405，于是出现：
「同步服务不可用：当前本地服务没有同步代理……」

解决办法：**把代理单独部署成一个公网可达、且开启 CORS 的服务**，让浏览器跨域调用它。
密钥（Webhook 地址）只放在代理服务端，绝不下发浏览器——比本地方案更安全。

---

## 方案 A（推荐）：Cloudflare Worker，免费、无需信用卡

1. 打开 https://dash.cloudflare.com/ → **Workers & Pages** → 创建 Worker。
2. 用本项目根目录的 `cloudflare-worker.js` 内容覆盖编辑器，部署。
   得到地址形如：`https://bfy-sync.<你的子域>.workers.dev`
3. Worker 的 **Settings → Variables** 里添加一个 **Secret**（不是普通变量）：
   - 名称：`WECOM_WEBHOOK`
   - 值：企业微信智能表格的 Webhook 完整地址
4. 把地址拼上 `/api/wecom-sync`，填进前端 `assets/config.js` 的 `PROXY_URL`：
   ```js
   var PROXY_URL = 'https://bfy-sync.<你的子域>.workers.dev/api/wecom-sync';
   ```
5. 重新 `git push`（GitHub Pages 会自动重新构建），公网同步即可用。

> 收紧调用来源（可选）：把 `cloudflare-worker.js` 里 `corsHeaders` 的 `origin` 改成固定值
> `'https://demonxiaodi.github.io'`，避免被其他站点冒用。

---

## 方案 B：把 server.js 部署到 Render / Railway / Fly 等 Node 平台

`server.js` 已内置 CORS（跨域可调用）并支持环境变量 `PORT`。

1. 把本项目（含 `server.js`、`package.json`）推到一个仓库。
2. 在 Render / Railway 新建 Web Service，构建命令留空，启动命令 `node server.js`。
3. 配置环境变量：
   - `PORT`：平台会自动注入（server.js 已读取 `process.env.PORT`）。
   - `WECOM_WEBHOOK`：填企业微信 Webhook 完整地址（等价于本地的 `server-config.json`，但部署时用环境变量更安全）。
4. 部署后得到公网地址 `https://<你的服务>.onrender.com`，拼接 `/api/wecom-sync` 填进 `assets/config.js` 的 `PROXY_URL`。
5. 重新部署前端。

---

## 本地仍然怎么用

完全不变：双击 `start.bat` 或 `node server.js`，访问 `http://127.0.0.1:5180/index.html`。
`assets/config.js` 检测到本地域名会自动走同源 `/api/wecom-sync`，不走公网代理。

---

## 排查清单

| 现象 | 原因 / 处理 |
|---|---|
| 公网访问提示 HTTP_405 | 代理没部署，或 `config.js` 的 `PROXY_URL` 仍是占位 `YOUR-WORKER-SUBDOMAIN` |
| 提示「网络异常」 | 代理地址不可达 / Worker 未部署 / CORS 仍被拦（确认 Worker 返回了 `Access-Control-Allow-Origin`） |
| 提示「服务端未配置 WECOM_WEBHOOK」 | 代理侧没设密钥（Worker Secret 或环境变量 `WECOM_WEBHOOK`） |
| 本地能写、公网不能写 | 本地走 server.js 同源代理；公网必须按本文部署独立代理 |
